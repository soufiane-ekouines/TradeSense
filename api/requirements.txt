# Flask and extensions
Flask==3.0.0
Flask-Cors==4.0.0

# Turso / LibSQL database driver - use HTTP API directly
# libsql-client has issues with result parsing, so we use requests
requests==2.31.0

# Authentication
PyJWT==2.8.0
werkzeug==3.0.1

# Environment variables
python-dotenv==1.0.1

# Web scraping for news (lightweight)
beautifulsoup4==4.12.3
lxml==5.1.0

yfinance>=0.2.28